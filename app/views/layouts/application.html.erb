<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
​
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
​
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
​
    <%= csrf_meta_tags %>
  </head>
​
  <body class="controller-<%= controller_name %>">

    <nav class="navbar navbar-default topnav" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button class="navbar-toggle" data-target="#bs-example-navbar-collapse-1" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand brand">
        <% if !(controller_name == 'home' && action_name == 'index') %>
            <%= link_to image_tag("logo-1.png", :alt => "logo"), game_index_path%>
        <%end%>
      </div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling
    -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class = "nav navbar-nav navbar-left">
        <li>
          <%if !current_user.nil?%>
            <%=link_to "Leaderboard", game_leaderboard_path, :method => :get%>
          <%end%>
        </li>
        <li>
          <%if !current_user.nil?%>
            <%=link_to "My Leagues", leagues_index_path, :method => :get%>
          <%end%>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
​
        <li>
          <%if !current_user.nil?%>
            <%=link_to "My Profile", profile_path(current_user.id), :method => :get%>
          <%end%>
        </li>
        <li>
          <%if !current_user.nil?%>
            <%=link_to "Log out", destroy_user_session_path, :method => :delete%>
          <%end%>
        </li>
      </ul>
    </div>
  </nav>


<% if !(controller_name == "home" && action_name == "index") %>
  <% unless notice.nil? %>
      <p class="notice"><%= notice %></p>
<% end %>
<% unless alert.nil? %>
   <p class="alert"><%= alert %></p>
<% end %>
<% end %>

<div class="content-wrapper">
  <%= yield %>
</div>

<!-- <div class="navbar-bottom-wrapper"> -->

  ​<div class="navbar navbar-default navbar-bottom">
    <div class="container">
      <div class="row">
        <div class="col-sm-4">
          <ul class = "nav">
            <li>
          About
        </li><li>
          FAQ
        </li>
      </ul>
        </div>
      </div>
    </div>
  </div>

</div>

  </body>
</html>
